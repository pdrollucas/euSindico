# Modelo de variáveis de ambiente — nunca contém segredos (ver documentation/SECURITY.md, seção 8).
# Copie para .env.local e ajuste para o seu ambiente (não versionado).

VITE_API_BASE_URL=https://localhost:7091
